describe("loverocks install", function()
	local Install = require 'loverocks.commands.install'
	it("can be used by API", function()
	end)

	it("can add dependencies", function()
	end)

	it("can use custom servers", function()
	end)
end)
